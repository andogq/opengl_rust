pub mod traits;
mod vertex_array;
mod shader;

use traits::*;
use vertex_array::VertexArray;
use shader::Shader;

pub struct Engine {

}

impl Engine {
    pub fn new() -> Engine {
        Engine {

        }
    }
}